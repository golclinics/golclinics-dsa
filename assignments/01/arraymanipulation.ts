function arrayManipulation(n, queries) {
  let arr = [];
  let max = 0;

  // n is the size of array > defined by  n and queries.length
  for (let l = 0; l < n; l++) {
    arr[l] = 0;
  }
  // TODO > figure this out
  return max;
}

let queries = [
  [1, 5, 3],
  [4, 8, 7],
  [6, 9, 1],
];

console.log(arrayManipulation(10, queries));
