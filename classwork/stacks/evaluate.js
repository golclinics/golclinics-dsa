const math = (s) => {
 s.split("")
 let calulation;
 let sign;
 for (let i = 0; i < s.length; i++) {
   if(s[i] == ("+" || "i" ){
     sign[i]=s[i];
   }
    calulations s[i]=i; 
 }
  return calulation;
}
